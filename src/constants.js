export const TILE_W = 64;
export const TILE_H = 32;
export const MAP_COLS = 30;
export const MAP_ROWS = 30;
export const PLAYER_SPEED = 120; // pixels per second
export const TREE_DENSITY = 0.12;
export const SPAWN_EXCLUSION = 4; // tiles around spawn without trees

export const ORIGIN_X = (MAP_COLS * TILE_W) / 2;
export const ORIGIN_Y = TILE_H * 2;
